<UserControl
    x:Class="JoyLeeWrite.Views.WriteView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:JoyLeeWrite.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="clr-namespace:JoyLeeWrite.Utils"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <DropShadowEffect
            x:Key="DropShadowEffect"
            BlurRadius="10"
            Direction="270"
            ShadowDepth="2"
            Color="#22000000" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border
            Grid.Row="0"
            Margin="30,20,30,40"
            Padding="25"
            Background="#F8FBFF"
            CornerRadius="6"
            Effect="{DynamicResource DropShadowEffect}">
            <RichTextBox
                x:Name="EditorRichTextBox"
                Padding="0"
                VerticalAlignment="Top"
                utils:RichTextHelper.BoundDocument="{Binding Content, Mode=TwoWay}"
                AcceptsReturn="True"
                Background="Transparent"
                BorderThickness="0"
                FontSize="16"
                Foreground="Black" />
        </Border>
        <TextBlock
            Grid.Row="1"
            Margin="30,0,0,25"
            FontSize="13"
            Foreground="#666"
            Text="{Binding WordCount, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
    </Grid>
</UserControl>
