<Window
    x:Class="JoyLeeWrite.Views.ForgotPasswordDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:JoyLeeWrite.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="clr-namespace:JoyLeeWrite.Utils"
    xmlns:vm="clr-namespace:JoyLeeWrite.ViewModels"
    Title="Reset Password"
    Width="450"
    Height="750"
    Background="White"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Window.DataContext>
        <vm:ForgotPasswordViewModel />
    </Window.DataContext>

    <Window.Resources>
        <!--  Style cho TextBox  -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#E0E0E0" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2B7DE9" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!--  Style cho PasswordBox  -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#E0E0E0" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2B7DE9" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!--  Style cho Button chính  -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2B7DE9" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E5FC4" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC" />
                    <Setter Property="Cursor" Value="Arrow" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!--  Style cho Button phụ  -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#F5F5F5" />
            <Setter Property="Foreground" Value="#666666" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="20,12" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Padding="30,25">
        <StackPanel>
            <!--  Header  -->
            <TextBlock
                Margin="0,0,0,8"
                FontSize="24"
                FontWeight="Bold"
                Foreground="#333333"
                Text="Reset Your Password" />
            <TextBlock
                Margin="0,0,0,25"
                FontSize="13"
                Foreground="#999999"
                Text="Enter your email to receive a verification code"
                TextWrapping="Wrap" />

            <!--  Email Input  -->
            <TextBlock
                Margin="0,0,0,8"
                FontSize="13"
                FontWeight="Medium"
                Foreground="#333333"
                Text="Email Address" />
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox
                    Grid.Column="0"
                    Style="{StaticResource ModernTextBox}"
                    Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
                <Button
                    Grid.Column="1"
                    Width="80"
                    Margin="10,0,0,0"
                    Padding="16,10"
                    Command="{Binding SendVerificationCodeCommand}"
                    Content="Send Code"
                    Style="{StaticResource PrimaryButton}" />
            </Grid>

            <!--  Verification Code Input  -->
            <TextBlock
                Margin="0,0,0,8"
                FontSize="13"
                FontWeight="Medium"
                Foreground="#333333"
                Text="Verification Code" />
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox
                    Grid.Row="0"
                    Width="160"
                    Margin="0,0,0,20"
                    Style="{StaticResource ModernTextBox}"
                    Text="{Binding VerificationCode, UpdateSourceTrigger=PropertyChanged}" />
                <Button
                    Grid.Column="1"
                    Width="80"
                    Margin="10,0,0,20"
                    Padding="16,10"
                    Command="{Binding VerificationCodeCommand}"
                    Content="Verify Code"
                    Style="{StaticResource PrimaryButton}" />
            </Grid>
            <!--  New Password Input  -->
            <TextBlock
                Margin="0,0,0,8"
                FontSize="13"
                FontWeight="Medium"
                Foreground="#333333"
                Text="New Password" />
            <PasswordBox
                x:Name="NewPassword"
                Margin="0,0,0,20"
                utils:PasswordHelper.BoundPassword="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                IsEnabled="{Binding IsCodeSent}"
                Style="{StaticResource ModernPasswordBox}" />

            <!--  Confirm Password Input  -->
            <TextBlock
                Margin="0,0,0,8"
                FontSize="13"
                FontWeight="Medium"
                Foreground="#333333"
                Text="Confirm New Password" />
            <PasswordBox
                x:Name="ConfirmPassword"
                Margin="0,0,0,25"
                utils:PasswordHelper.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                IsEnabled="{Binding IsCodeSent}"
                Style="{StaticResource ModernPasswordBox}" />

            <!--  Error/Success Message  -->
            <TextBlock
                Margin="0,0,0,20"
                FontSize="12"
                Foreground="{Binding MessageColor}"
                Text="{Binding Message}"
                TextAlignment="Center"
                TextWrapping="Wrap"
                Visibility="{Binding MessageVisibility}" />

            <!--  Action Buttons  -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button
                    Grid.Column="0"
                    Height="40"
                    Margin="0,0,5,0"
                    Click="Cancel_Click"
                    Content="Cancel"
                    Style="{StaticResource SecondaryButton}" />
                <Button
                    Grid.Column="1"
                    Margin="5,0,0,0"
                    Command="{Binding ResetPasswordCommand}"
                    Content="Reset Password"
                    Style="{StaticResource PrimaryButton}" />
            </Grid>
        </StackPanel>
    </Border>
</Window>